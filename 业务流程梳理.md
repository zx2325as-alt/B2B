# 会话页与人物信息页业务流程完整梳理

## 1. 文档目标

本文只聚焦两个核心页面：

- 页面一：会话页，对应前端路由 `/chat`
- 页面二：人物信息页，对应前端路由 `/admin`

这两个页面不是独立系统，而是同一套业务闭环的前后两个阶段：

1. 在会话页中产生对话、分析、关系变化和行为线索
2. 这些结果沉淀到人物信息页对应的角色档案、事件、关系、观察记录中
3. 人物信息页维护后的角色资料，又会反向作为下一轮会话分析的上下文输入

所以整个系统的本质不是“一个聊天页 + 一个管理页”，而是“会话驱动建模，建模反哺会话”的双向闭环。

---

## 2. 两个页面的职责区分

### 2.1 会话页负责什么

会话页负责实时发生中的业务过程，核心目标是：

- 让用户选择会话、场景和发言角色
- 输入一条角色发言
- 后端基于角色档案、关系、历史上下文和心理状态进行 AI 分析
- 前端流式展示分析结果
- 最后把本轮对话结果沉淀为可归档、可回看、可继续演化的数据

它更偏“运行态”。

### 2.2 人物信息页负责什么

人物信息页负责角色建模与资料管理，核心目标是：

- 维护角色基础信息
- 展示 AI 聚合后的角色档案
- 管理事件时间线
- 管理人物关系网络
- 审核 AI 自动更新建议
- 通过导入和导出完成外部数据接入与数据沉淀

它更偏“静态知识库”和“结果管理中心”。

### 2.3 为什么要把两个页面拆开

这样拆分是合理的，原因有四个：

- 会话过程是高频交互，强调即时反馈、流式输出和上下文连续性
- 人物信息维护是低频编辑，强调结构化、可审查、可追溯
- 如果把所有功能堆在同一个页面，用户会同时面对聊天、分析、建模、关系编辑、事件维护，界面会非常重
- 两个页面拆开后，系统形成明确边界：会话页负责“产生行为数据”，人物信息页负责“沉淀结构化认知”

---

## 3. 系统整体架构

## 3.1 前端技术栈

- Vue 3：页面与组件框架
- Vue Router：页面路由管理
- Pinia：前端状态管理
- Axios：常规 HTTP 请求
- Fetch + ReadableStream：会话页 SSE 流式消息接收
- Vite：前端开发与构建工具
- ECharts / vue-echarts：情绪趋势、统计类图表能力
- D3：关系网络和图形可视化能力

### 3.2 后端技术栈

- FastAPI：后端 API 服务
- SQLAlchemy：数据库 ORM
- Pydantic：请求响应结构校验
- SQLite：当前项目默认本地持久化存储
- OpenAI 兼容 SDK：统一接入 DeepSeek / OpenAI / Ollama / vLLM 等模型源
- httpx：异步 HTTP 客户端
- YAML：AI 模型路由和供应商配置
- BackgroundTasks：导入预览增强、导入提交等后台任务

### 3.3 AI Harness 能力层

后端不是直接把请求发给模型，而是封装了一层 AI Harness，核心模块包括：

- Orchestrator：统一 AI 编排入口
- ModelRouter：按任务复杂度分配模型
- Guardrails：校验 AI 输出格式和安全性
- ContextManager：管理历史上下文与 token 预算
- StateEngine：维护角色心理状态
- ConsistencyEngine：给分析增加人格一致性约束
- ImportEngine：把外部文本解析为系统统一协议

这样写的原因是把“页面业务逻辑”和“模型调用细节”拆开，避免前后端业务代码直接绑死某一家模型厂商。

---

## 4. 核心数据对象

系统主要围绕下面几类数据组织：

- Character：角色主档案
- CharacterEvent：角色事件时间线
- Relationship：角色与角色之间的关系
- CharacterObservation：AI 生成的更新建议、行为模式、审核记录
- Conversation：一次会话容器
- Message：会话中的具体消息与分析结果
- ImportFile：导入文件记录
- InteractionUnit：导入后的交互单元，作为事件、关系、消息的中间协议

这样设计的好处是：

- 会话数据和人物数据既有关联，又不会混在一张表里
- 支持“先聊天，后归档”
- 也支持“先导入文本，后生成只读会话”
- 所有 AI 推断都可以通过 Observation 或 InteractionUnit 保留追溯痕迹

---

## 5. 会话页完整业务流程

## 5.1 页面初始化流程

用户进入 `/chat` 后，前端会并行做两件事：

- 加载会话列表
- 加载角色列表

如果已有会话，就默认选中第一个会话并拉取消息。

如果没有会话，就按预设场景自动准备默认角色。

这样写的原因是：

- 左侧会话列表和角色选择都依赖初始化数据
- 会话页需要同时知道“当前有哪些会话”和“有哪些角色可参与会话”
- 默认场景预设能让页面首次进入时不至于空白

## 5.2 用户发送消息流程

用户在会话页发送一条消息时，流程如下：

### 第一步：前端校验

前端先校验：

- 当前是否是只读导入会话
- 是否已经选择发言角色
- 输入内容是否为空

这样写的原因是防止非法输入和错误操作直接进入后端。

### 第二步：前端乐观更新

Pinia 中的 chat store 会先插入两条临时消息：

- 一条用户消息
- 一条“AI分析”占位消息

这样写的原因是提升交互体验，避免用户发出消息后页面毫无反馈。

### 第三步：流式请求后端

前端通过 `fetch('/api/v1/chat/send')` 发起请求，并持续读取流式返回。

返回协议分为三类：

- `delta`：增量文本
- `done`：完整分析结果
- `error`：异常信息

这样写的原因是会话分析文本可能较长，流式展示比一次性等待整段返回更适合聊天场景。

### 第四步：后端接收消息并建立本轮分析上下文

后端收到消息后不会直接调用模型，而是先做上下文组装：

1. 找出当前发言者
2. 从活动角色中推断主要聆听者
3. 读取发言者与聆听者的人物档案
4. 读取最近对话历史
5. 读取两人的关系快照
6. 读取角色行为模式与稳定人格
7. 通过状态机初始化双方心理状态
8. 通过一致性约束器生成本轮人格约束说明

这一步是系统最关键的业务价值。

因为这个系统不是普通聊天系统，而是“代入人物关系的深层心理分析系统”。如果不先拼出这一层上下文，AI 只会输出普通文本，无法体现角色长期一致性与关系张力。

### 第五步：先写入用户消息，再进行 AI 编排

后端先把用户消息落库，然后调用 Orchestrator。

Orchestrator 采用两阶段策略：

- 阶段一：生成表层回复
- 阶段二：生成结构化分析结果

最终会输出：

- reply：表层表达
- inner_monologue：内心独白
- emotion_label：多层情绪标签
- emotion_score：情绪强度
- subtext：潜台词、策略、长期动机
- psychological_tag：心理标签

之所以这样写，而不是一次性直接让模型全输出，是因为：

- 表层回复适合流式展示
- 分析层适合结构化持久化
- 两段式比单次混合生成更容易控制质量和格式

### 第六步：回写分析结果

分析完成后，后端会：

- 更新用户消息上的意图、策略、情绪字段
- 新增一条 AI 分析消息
- 把分析结果写回心理状态机
- 把本轮内容加入 ContextManager，供下一轮继续使用

这样形成连续会话记忆，而不是每轮都重新开始。

## 5.3 会话页的扩展流程

### 1. 新建会话

用户可以创建新会话，并绑定场景。

为什么这样写：

- 场景决定对话背景
- 新会话天然应该拥有自己的上下文和心理状态空间

### 2. 切换场景

切换场景时会创建新会话并应用默认角色。

为什么不直接修改当前会话：

- 场景切换本质上意味着世界观和交互目标变化
- 如果继续复用旧会话，会污染上下文

### 3. 删除会话

删除会话时，前后端同步删除会话及其消息。

为什么这样写：

- Conversation 与 Message 是一组强绑定数据
- 删除容器时需要清理子消息，避免脏数据残留

### 4. 分支回退

用户可以从某条消息创建分支。

实现方式是：

- 以前端截断消息显示为主
- 后端重置 ContextManager 中该消息之后的上下文

为什么这样写：

- 当前项目更关注重新推演，而不是复杂的真树状版本管理
- 这样实现成本低，同时满足“从某一节点重新分析”的业务需求

### 5. 情绪张力分析

会话页支持按角色对查看情绪张力，包括：

- 试图激发的情绪
- 深层情绪
- 策略轨迹
- 热力图
- 情绪关键词

为什么这样写：

- 单看一句回复不容易看出关系演化
- 情绪张力面板把离散分析变成可视化趋势，更适合观察长期互动变化

### 6. 历史消息补全分析

如果历史用户消息缺少分析层，可以单独补做一次分析。

为什么这样写：

- 历史数据可能来自旧版本或导入流程
- 补全分析能把旧数据纳入当前统一协议

### 7. 归档到人物信息

当前会话可以一键归档到角色档案。

归档时会做三类事情：

- 生成角色事件
- 更新关系强度和情感极性
- 抽取行为模式与潜在观察建议

这是页面一和页面二真正连起来的地方。

---

## 6. 人物信息页完整业务流程

## 6.1 页面初始化流程

用户进入 `/admin` 后，前端会先拉取：

- 全量角色
- 全量关系

如果路由上带 `charId`，就自动打开该角色。

选中角色后，再继续加载：

- 事件列表
- 观察记录
- 聚合后的角色档案视图
- AI 更新摘要

为什么不一次把所有内容全量拉完：

- 角色很多时事件和观察会很重
- 列表页只需要轻量信息
- 详情数据按当前角色延迟拉取更合适

## 6.2 人物信息页的四层数据结构

人物信息页不是只显示 Character 表本身，而是四层叠加：

### 第一层：基础角色信息

包含：

- 名称
- 身份角色
- 背景
- 年龄
- 人格标签
- 动机
- 弱点
- 说话风格

这是人工和 AI 都会维护的主档案层。

### 第二层：聚合档案视图

后端通过 `_build_profile_view()` 把多个数据源汇总成统一结构：

- 基础信息
- 人格模型
- 行为模式
- 核心动机
- 核心弱点
- 说话风格
- 关系网络
- 事件时间线

为什么这样写：

- 前端展示不应该自己拼装多张表
- 聚合结构统一后，UI 更稳定，后续导入与 AI 更新也能复用同一协议

### 第三层：观察记录

Observation 是系统的“可审核 AI 中间层”。

它记录：

- 哪个字段要更新
- 旧值是什么
- 新值是什么
- 依据是什么
- 当前状态是待确认、已应用还是已忽略

为什么这样写：

- AI 生成内容不能直接无条件覆盖人物档案
- 需要把 AI 推断和最终数据变更分离

### 第四层：关系与事件

关系和事件是人物页最重要的结构化沉淀：

- 事件负责记录人物经历
- 关系负责记录人与人之间的动态变化

这两者又会反向影响会话页中的角色心理建模。

## 6.3 人物信息页的核心操作流程

### 1. 创建角色

创建角色时，后端先保存基础信息，再尝试自动生成 AI 心理档案。

自动补全内容包括：

- personality_tags
- core_traits
- weakness
- motivation
- speaking_style

为什么这样写：

- 用户只需要先输入最小必要信息
- AI 自动补全能降低建模成本
- 即使 AI 失败，也不影响角色创建成功

### 2. 编辑角色

编辑角色时，前端会把基础字段和行为模式一起编辑，再同步回后端。

为什么要把行为模式单独做 CRUD：

- 行为模式不是 Character 主表原生字段
- 它本质上属于 Observation 的一种结构化结果
- 单独维护能保留来源、置信度、触发条件、示例等附加信息

### 3. AI 自动更新

用户点击 AI 自动更新后，后端会基于最近事件摘要构建简化对话，再调用 AI 生成建议。

这些建议不会直接落到角色主档案，而是先写成 Observation。

为什么这样写：

- 这样更安全
- 也更符合“AI 建议，人来审”的业务逻辑

### 4. 审核 Observation

当用户批准某条 Observation 时，后端会把 `new_value` 应用到角色对应字段。

为什么这样写：

- 批准动作是最终生效点
- 这样可以清楚区分“建议”和“已采用”

### 5. 事件管理

人物页支持新增和删除事件。

为什么事件要独立：

- 会话归档会生成事件
- 用户也可能补录人物过去经历
- 事件既服务展示，也服务 AI 更新建议

### 6. 关系管理与关系分析

人物页支持新增、编辑、删除关系，还支持让 AI 分析关系趋势。

关系分析会结合历史强度与情感变化进行总结。

为什么这样写：

- 关系不是静态标签
- 需要保留 history，才能看见关系是如何演化的

### 7. 导入与导出

人物页还是统一的数据接入口。

导入分两段：

- 先预览
- 再确认提交

导入时可选：

- 是否创建只读会话
- 是否自动归档
- 使用哪种场景

为什么这样写：

- 导入数据通常不完全可靠
- 预览让用户先确认角色映射与解析结果
- 只读会话能把外部文本转成页面一可查看但不可继续编辑的消息流

---

## 7. 两个页面是如何联动的

这部分是整个业务最重要的主线。

## 7.1 从人物信息页到会话页

人物信息页会把这些内容提供给会话页：

- 角色基础档案
- 人格标签与核心特质
- 动机与弱点
- 行为模式
- 关系网络
- 关键事件

会话页发送消息时，后端会把这些内容取出来，作为本轮分析的角色记忆和关系背景。

所以人物信息页的价值，不只是“展示资料”，而是给会话页提供高质量上下文。

## 7.2 从会话页到人物信息页

会话页又会把这些内容回流到人物信息页：

- 对话归档生成事件
- 对话归档更新关系
- 对话分析抽取行为模式
- 发现人格冲突时生成 Observation

所以会话页的价值，不只是“聊天”，而是在持续生产人物建模素材。

## 7.3 通过导入流程形成第三条通路

导入流程同时连接两个页面：

- 导入文本先进入人物信息页进行预览、映射和审核
- 提交后可生成角色、事件、关系和交互单元
- 如果启用了创建只读会话，还会直接生成会话页可读消息

这意味着系统支持三种建模入口：

- 手工创建角色
- 实时会话沉淀角色
- 外部文本导入重建角色

---

## 8. 所有关键功能点清单

## 8.1 会话页功能点

- 会话列表加载
- 新建会话
- 删除会话
- 场景切换
- 角色选择
- 自定义角色加入会话
- 指定发言角色
- 流式发送消息
- 中途取消流式生成
- 展示 AI 分析层
- 历史消息补全分析
- 分支回退
- 情绪张力分析
- 只读导入会话保护
- 一键归档到人物档案

## 8.2 人物信息页功能点

- 角色列表浏览与搜索
- 创建角色
- 编辑角色
- 删除角色
- AI 自动补全初始档案
- 统一 8 模块档案展示
- 行为模式增删改
- 观察记录查看
- Observation 审核
- AI 更新摘要统计
- 事件新增与删除
- 关系新增、编辑、删除
- 关系 AI 分析
- 文件导入预览
- 导入后台轮询
- 导入确认提交
- 导出完整 JSON

## 8.3 跨页面功能点

- 会话归档沉淀人物数据
- 人物档案反哺会话分析
- 导入生成只读会话
- AI 更新记录独立查看
- 数据统一导出

---

## 9. 关键实现方式拆解

## 9.1 为什么前端要用 Pinia

会话页和人物页都有明显的共享状态需求：

- 当前会话
- 当前消息列表
- 角色列表
- 关系列表
- 当前选中的角色

用 Pinia 的好处是：

- 页面切换时状态更清晰
- 组件内部不需要层层传参
- 会话状态和角色状态有明确边界

## 9.2 为什么会话发送使用 SSE 流式而不是普通请求

因为 AI 分析输出较长，且用户对“立即看到内容开始生成”非常敏感。

如果改成普通请求：

- 用户需要完整等待
- 页面反馈延迟明显
- 中途无法取消

SSE 流式更适合会话体验。

## 9.3 为什么后端要保留 Conversation 和 Message 两层

因为会话容器和会话消息是两种不同维度：

- Conversation 表示一段独立上下文空间
- Message 表示这段上下文里的每一轮发言和分析

这样才能支持：

- 新建多个会话
- 删除单个会话
- 对单个会话做归档
- 对单条消息做补分析和分支

## 9.4 为什么 AI 建议不直接覆盖人物数据

项目采用 Observation 作为中间层，原因是：

- AI 有概率误判
- 人物档案属于核心资产，不适合直接覆盖
- 需要让用户看到更新依据和置信度

这本质上是一种“可审计 AI”设计。

## 9.5 为什么角色档案要聚合成统一 8 模块

统一 8 模块的好处是：

- 前端展示结构稳定
- 导入预览、人物详情、AI 更新记录可以共用同一套模块语言
- 新增字段时只需要考虑归属哪个模块，而不是重新设计页面布局

## 9.6 为什么关系和事件要分开存储

因为两者表达的是不同事实：

- 事件是人物经历
- 关系是人物之间的长期互动状态

如果把两者混在一起，后面做趋势分析和归档更新会很混乱。

## 9.7 为什么要有心理状态机

如果没有状态机，每一轮 AI 分析只能依赖当前 prompt，角色会容易漂。

状态机的价值在于：

- 保留上一轮策略
- 保留情绪向量
- 保留关系压力
- 保留稳定人格和行为模式

这样多轮会话更像连续人物互动，而不是单轮问答。

## 9.8 为什么要有模型路由和输出护栏

这是为了稳定性和可扩展性：

- 不同任务复杂度不同，没必要都用同一配置
- JSON 输出任务必须校验格式
- AI 返回异常格式时要尽早拦截
- 后续更换模型供应商时代价更低

---

## 10. 一条完整闭环示例

下面用一条真实业务路径说明整个系统如何运转。

### 场景：用户在会话页让“吉娜”对“马可”说一句话

1. 前端会话页中，用户选择场景和角色
2. 用户输入发言内容，前端通过 store 发起流式请求
3. 后端读取吉娜与马可的人物档案、关系、事件和近期上下文
4. AI Harness 先生成表层回复，再生成深层分析
5. 分析结果回写为 AI 消息，并更新心理状态
6. 用户继续多轮对话，系统不断累积消息与分析
7. 用户点击归档
8. 后端把吉娜相关对话整理成事件时间线
9. 同时根据互动结果更新吉娜对马可的关系强度与情感极性
10. 如果识别出稳定策略，还会生成行为模式 Observation
11. 用户切到人物信息页查看吉娜
12. 页面中会看到新的事件、变化后的关系、待确认的 AI 更新建议
13. 用户批准建议后，角色主档案被正式更新
14. 下一次再回到会话页，新的角色档案又会参与后续分析

这就是该项目的完整业务闭环。

---

## 11. 总结

一句话总结：

这个项目的核心不是“聊天”，也不是“角色管理”，而是把人物互动过程转化成结构化人格资产，并让这些资产持续反过来提升下一轮互动质量。

如果再压缩成两句话：

- 会话页负责生产行为数据和心理分析结果
- 人物信息页负责沉淀、修正、审核和管理这些结果

因此两个页面的关系是：

- 不是并列关系
- 也不是主从关系
- 而是前台运行态与后台建模态的闭环关系

---

## 12. 对应代码入口

- 前端应用入口：`frontend/src/main.js`
- 路由入口：`frontend/src/router/index.js`
- 会话页：`frontend/src/views/ChatUI.vue`
- 人物信息页：`frontend/src/views/AdminDashboard.vue`
- AI 更新记录页：`frontend/src/views/AIUpdateLog.vue`
- 会话状态：`frontend/src/stores/chat.js`
- 角色状态：`frontend/src/stores/characters.js`
- 前端接口封装：`frontend/src/api/index.js`
- 会话接口：`backend/app/api/chat.py`
- 人物接口：`backend/app/api/characters.py`
- 数据模型：`backend/app/models/sql_models.py`
- AI 编排核心：`backend/app/harness/orchestrator.py`
- 上下文管理：`backend/app/harness/context_manager.py`
- 心理状态机：`backend/app/harness/state_engine.py`
- 模型路由与护栏：`backend/app/harness/model_router.py`

