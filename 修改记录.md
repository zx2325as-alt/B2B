# 项目修改记录

## 2026-04-08 启动报错修复

### 1. 修复后端启动报错 (NameError: name 'Any' is not defined)

- **修改说明**: 在 `backend/app/harness/model_router.py` 文件中，将底部的 `from typing import Any` 移动到了文件顶部。
- **为什么要这样修改**: Python 是从上到下执行代码的。在 `OutputGuardrails.extract_json` 方法定义时，类型提示使用了 `Any`
  ，但此时 `Any` 还未被导入，导致 `NameError` 报错。将导入语句移到文件顶部即可解决此问题。另外由于后端还缺少了 `anthropic`
  等依赖库，一并执行了 `pip install -r requirements.txt`。
- **利用的技术**: Python 类型提示 (`typing`) 和包管理工具 `pip`。

### 2. 修复前端启动报错 (Cannot find module './dist/compiler-core.cjs.js')

- **修改说明**: 删除了 `frontend/node_modules` 文件夹，并重新运行 `npm install` 安装所有前端依赖。
- **为什么要这样修改**: 该错误通常是因为 `node_modules` 目录下的依赖包损坏或不完整（可能是因为安装中断或环境切换）。通过彻底删除并重新安装，可以恢复完整正确的依赖包结构，使
  Vite 开发服务器正常启动。

### 3. 修复依赖包冲突 (pip dependency resolver conflicts)

- **修改说明**: 使用 `pip` 手动升级了 `fastapi`, `python-multipart`, `starlette`, `langchain`, `protobuf`
  等冲突依赖，使其版本满足环境中其他库（如 `gradio`, `langchain-community`, `grpcio-tools` 等）的最低要求。
- **为什么要这样修改**: 在安装后端 `requirements.txt` 时，原有的固定版本号导致与环境中已有的其它包（例如全局安装的
  langchain 或 gradio）产生了版本不兼容冲突。通过升级这些包，可以消除 pip 解析依赖时的报错，保持环境依赖的健康与一致。
- **利用的技术**: Python 包管理工具 `pip` 的依赖解析与版本约束机制。

### 4. 修复 API 连接失败及增加 YAML 配置文件支持 (APIConnectionError)

- **修改说明**:
    1. 在 `backend/app/` 下新建了 `conf` 文件夹及 `config.yaml` 文件，用于存放 `anthropic` 相关的 `api_key` 和 `base_url`
       配置。
    2. 修改了 `backend/app/harness/orchestrator.py` 文件，增加了读取 YAML
       配置的逻辑。现在会优先读取环境变量中的 `ANTHROPIC_API_KEY`，然后再从 `config.yaml`
       中读取补充配置，并在初始化 `AsyncAnthropic` 客户端时动态传入 `api_key` 和 `base_url` 等参数。
- **为什么要这样修改**: 原本代码直接初始化 `anthropic.AsyncAnthropic()`，如果在国内或特定网络环境下直接访问 Anthropic API
  经常会产生 `APIConnectionError` 导致超时断开。通过引入 `config.yaml`
  ，我们可以灵活配置国内代理或中转接口地址 (`base_url`)，并且将配置独立存放，方便环境隔离与网络问题排查。
- **利用的技术**: Python 的 `yaml` 解析库以及 `pathlib` (用于路径拼接)；通过 kwargs 动态传递初始化参数配置 API 客户端。

### 5. 替换 Anthropic SDK 为 OpenAI SDK 以支持多模型源 (DeepSeek, OpenAI, Ollama, vLLM 等)

- **修改说明**:
    1. 重构了 `backend/app/conf/config.yaml` 配置文件，将其改为以 `ai` 为根节点，包含 `default_provider`
       以及 `providers` (支持 openai, deepseek, ollama, vllm 等多个配置)，每个 provider 下都有对应的 `base_url`、`api_key`
       和 `models` (区分 simple, medium, complex 模型)。
    2. 修改了 `backend/app/harness/model_router.py`，从 `config.yaml` 中读取当前选用 provider 的具体模型名称，替换掉原来硬编码的
       Claude 模型。
    3. 修改了 `backend/app/harness/orchestrator.py`，将底层的 `anthropic.AsyncAnthropic`
       客户端完全替换为 `openai.AsyncOpenAI`。并将原先使用 `client.messages.create` 和 `client.messages.stream` (
       Anthropic 独有格式) 的代码改写为 OpenAI 兼容的 `client.chat.completions.create`。
- **为什么要这样修改**: 因为大部分第三方模型厂商（包括国内的 DeepSeek、开源的 vLLM 和 Ollama 等）都提供了 OpenAI
  兼容的接口。通过将底层调用统一为 OpenAI SDK，我们只需在配置中切换 `base_url`、`api_key`
  和模型名称，就能无缝切换国内/国外/本地的各种大模型，大大增强了项目的可扩展性。
- **利用的技术**: `openai` Python SDK 的异步调用及流式输出 (`AsyncOpenAI`)、YAML 结构化配置解析。

### 6. 修复 UI 交互与大模型流式解析问题

- **修改说明**:
    1. **角色配置支持删除**: 优化了 `ChatUI.vue`
       中的角色选择逻辑。现在点击“角色管理”时，会直观展示“当前对话角色”，并支持一键删除任何角色（包括用户或自定义添加的角色）。同时，勾选和取消勾选“已有角色”会实时同步到当前对话中。
    2. **智能对话支持删除关闭**: 在 `ChatUI.vue`
       的左侧“对话列表”中，为每个会话选项增加了删除按钮（✕），并在后端 `api/chat.py`
       补充了对应的 `DELETE /conversations/{id}` 级联删除接口。
    3. **修复页面布局未占满主框**: 修正了 `App.vue` 中根布局 `.app-layout` 和 `.main-content` 缺失的 Flexbox
       样式，使得聊天主界面能够被强制约束并在主视口内正确伸展和滚动。
    4. **修复 AI 分析结果无法正常返回**: 修正了 `orchestrator.py` 中由于兼容 OpenAI SDK 产生的异常流解析问题。由于
       DeepSeek 或 OpenAI 在流式输出的最后一个 chunk 的 `choices` 可能为空，直接取 `choices[0]`
       会触发数组越界并被抛到前端导致分析失败。通过在解析循环中跳过空的 chunk 并适配推理模型（如 `deepseek-reasoner`
       ）的 `reasoning_content`，现已能够正确提取内容交由 JSON 护栏解析。
- **为什么要这样修改**: 原本的逻辑在很多边界条件（如不选角色、删除会话、处理不同的流式响应结构）下处理不周，导致交互体验差以及后端抛错。这些修改彻底打通了对话列表管理、角色管理以及流式数据的稳定渲染。
- **利用的技术**: Vue 3 响应式状态化 (`filter`, `map`)、CSS Flexbox 布局约束、Python 异步生成器 (`async generator`)
  及流式异常处理。

### 7. 修复前端 HTTP Proxy 代理连接被拒绝的问题 (ECONNREFUSED)

- **修改说明**:
    1. 将 `frontend/vite.config.js` 中的 proxy target 从 `http://localhost:8000` 修改为了 `http://127.0.0.1:8000`。
    2. 后台重新启动了后端 FastAPI 服务。
- **为什么要这样修改**: 当看到 `AggregateError [ECONNREFUSED]` 时，通常有两个原因：一是后端服务没有处于运行状态；二是
  Node.js 18+ 的网络解析会优先尝试解析 `::1` (IPv6)，而 FastAPI (uvicorn) 默认绑定在 IPv4 的 `127.0.0.1` 或 `0.0.0.0`，导致
  Vite 无法连接到后端的端口。修改为具体的 IP 地址 `127.0.0.1` 可以完美避开解析冲突，并确保前端能顺畅调通后端的 `/api/v1/*`
  接口。
- **利用的技术**: Node.js 网络请求底层解析、Vite 代理配置 (Proxy)。

### 8. 修复 AI 分析流式返回 Connection error (OpenAI 客户端底层连接拒绝)

- **修改说明**:
    1. 删除了 `start.bat` 中强制设置环境变量 `ANTHROPIC_API_KEY` 的逻辑。现在所有 API Key 都由 `config.yaml` 统一管理，避免环境变量与
       YAML 配置文件产生覆盖冲突。
    2. 在 `orchestrator.py` 的 OpenAI 客户端初始化过程中，显式注入了 `httpx.AsyncClient`
       ，并增加了连接超时和读写超时的限制配置 (`timeout=120.0, connect=30.0`)
       ，同时为了兼容可能存在的抓包工具或网络代理，启用了 `verify=False`（跳过 SSL 校验）。
- **为什么要这样修改**: OpenAI Python SDK 默认使用的底层网络库 `httpx` 有时会由于代理或 SSL
  证书导致拒绝连接（ECONNREFUSED/ConnectError）。此外，如果环境变量中仍然遗留了错误的 `ANTHROPIC_API_KEY`
  ，会干扰我们的凭证提取。清除干扰变量并增加底层的网络宽容度，可以极大提高 API 调用的成功率。
- **利用的技术**: OpenAI SDK 的 `http_client` 注入与 `httpx` 超时/SSL配置。

### 9. 修改 AI 分析逻辑 (站在聆听者立场进行深层分析)

- **修改说明**:
    1. **前端传递当前活动角色**: 修改了 `frontend/src/stores/chat.js` 和 `frontend/src/views/ChatUI.vue`
       ，在发送消息时（`/api/v1/chat/send`），将当前所有配置的活动角色 `activeCharacters` 传递给后端。
    2. **后端推断聆听者并构建记忆**: 在 `backend/app/api/chat.py` 中，收到请求后过滤掉当前说话的角色 (`speaker`)
       ，将其余角色认定为“聆听者”。然后从数据库获取这些聆听者的性格、动机、弱点等档案数据，构建为 `listener_memory`
       传递给分析模型，而不是像之前那样传递说话者自己的记忆。
    3. **更新分析提示词**: 重写了 `backend/app/harness/prompt_templates.py` 中的 `chat_analysis`
       模板。将系统设定的视角从“中立观察者”切换为“代入聆听者的立场”。现在 AI
       会结合聆听者的人设（如红猪、吉娜等）去感受对方的话语，并用第一人称输出聆听者那一刻真实的内心独白、情绪反应和对潜台词的解读。
- **为什么要这样修改**:
  原本的逻辑只是简单地分析说话者的情绪和潜台词，显得过于中立且平淡。而在沉浸式角色对话中，分析“听者如何理解和感受这句话”往往比“说者说了什么”更具戏剧张力和深度。这样修改不仅保留了现有的功能结构，还能显著提升
  AI 返回内容的带入感和人物互动体验，使整体逻辑更符合用户预期的双向关系演绎。
- **利用的技术**: FastAPI 模型扩展 (Pydantic `BaseModel` 添加字段)、Python 列表推导式与条件过滤、大语言模型角色扮演 Prompt
  Engineering (系统级预设与人称代入)。

### 10. 补完页面1核心功能 (场景切换 / 归档 / 增强情绪面板 / 补全分析)

- **修改说明**:
    1. **场景预设切换**: 在 `frontend/src/views/ChatUI.vue` 中加入场景切换弹层，提供“通用对话 / 老友酒吧闲聊 / 商务谈判 /
       HR面试 / 心理咨询”等预设。切换场景时会创建新会话、清空旧上下文，并自动加载场景默认角色与默认发言人。
    2. **发送前角色约束**: 前端发送消息前增加“必须先选择发言角色”的校验；未选角色时会阻止发送并提示。
    3. **一键归档**: 在 `backend/app/api/chat.py` 中新增 `POST /chat/conversations/{id}/archive`
       。归档时会自动为不存在的角色创建档案，并把对话文本、分析层（内心独白 / 情绪归因 / 策略动机 /
       标签）写入角色事件时间线，同时更新关系强度与情感极性，并尝试抽取行为模式进入观察建议。
    4. **历史消息补全分析**: 新增 `POST /chat/messages/{id}/reanalyze` 接口，前端对缺失分析层的历史消息显示“补全分析”按钮，点击后可重新生成该条消息的分析层。
    5. **增强情绪面板**: 情绪面板从单角色切换为“角色对”视角，新增 `/chat/conversations/{id}/emotion-tension`
       接口。除了表层情绪曲线，还返回深层情绪曲线与策略轨迹，前端可以直接点击点位或策略项跳回对应消息。
    6. **流式中断**: 在 `frontend/src/stores/chat.js` 与 `frontend/src/api/index.js` 中加入 `AbortController`
       ，允许在生成过程中主动中断当前流式任务，避免长输出阻塞后续操作。
- **为什么要这样修改**:
  之前页面1虽然有基础聊天能力，但场景、归档、历史补分析、情绪可视化这些关键能力没有闭环。补齐这些功能后，页面1才真正具备“沉浸式智能对话页”的完整闭环：能切场景、能分析、能归档、能回看、能追踪关系变化。
- **利用的技术**: Vue 3 响应式状态管理、FastAPI 路由扩展、SSE 流式中断控制 (`AbortController`)、关系强度增量更新与事件归档。

### 11. 升级 AI Harness 为“多主体心理博弈模拟引擎”

- **修改说明**:
    1. **新增心理状态机**: 新建 `backend/app/harness/state_engine.py`
       ，为每个会话中的每个角色维护持续运行的心理状态，包括情绪向量、当前目标、对他人信念、防御机制、关系权重、稳定人格、行为模式、上一轮意图与策略等。
    2. **新增一致性约束器**: 新建 `backend/app/harness/consistency_engine.py`
       ，在每次生成分析前，把“人格约束、关系约束、最近上下文约束”汇总为一致性规则，要求模型在分析中解释反常行为，避免角色突然失真。
    3. **回复生成改为两阶段**: 修改 `backend/app/harness/orchestrator.py`，先通过 `chat_surface_reply`
       模板流式生成“像真人的表层回复”，再通过 `chat_analysis` 模板生成结构化心理分析。这样既保留了流式体验，又让分析层包含完整的心理推理。
    4. **分析层升级**: `chat_analysis` 模板升级为“心理推理引擎”模式。`inner_monologue` 强制输出“第一反应 / 防御机制 /
       行为倾向”三层；`emotion_label` 改为“试图激发 / 表层 / 深层 / 压抑”四段情绪冲突结构；`subtext` 改为“短期策略 / 长期策略
       / 一致性说明”；`psychological_tag` 改为“主标签 / 次标签 / 关系标签”的高密度信号层。
    5. **状态回写**: 在流式分析完成后，`orchestrator.py` 会把本轮分析结果反向写入心理状态机，用于下一轮继续保持角色连续性和历史一致性。
- **为什么要这样修改**: 原先的 AI Harness
  仍偏向“单轮分析器”，角色更像一次性根据提示词做出回答。升级后，系统会把每条发言当作策略行为，并结合角色持续状态来推理“为什么这样想、为什么这样说、为什么会有偏差”，从而显著提升真实感、复杂度与长期一致性。
- **利用的技术**: AI Harness 上下文编排、分阶段 Prompt 策略（表层回复 + 结构化分析）、Python 状态机建模、可解释性一致性约束。

### 12. 页面3新增统一智能导入 / 导出入口，并接入 AI Harness 语义建模链路

- **修改说明**:
    1. **后端新增导入追溯模型**: 在 `backend/app/models/sql_models.py` 中新增 `ImportFile` 与 `InteractionUnit`
       两张表，用于记录导入文件、交互单元、置信度、分析层、追溯片段以及与事件/关系/消息的关联，不破坏原有角色、事件、关系表结构。
    2. **新增 AI Harness 导入解析引擎**: 新建 `backend/app/harness/import_engine.py`，负责统一读取 TXT / MD / PDF / DOCX
       / JSON / CSV，并自动识别结构化数据、对话文本、叙事文本。对话文本走规则解析 + AI 补全，叙事文本走 AI 统一解析，最终转成统一语义协议（角色
       / Interaction Unit / 事件 / 关系 / 剧情结构）。
    3. **新增导入相关 Prompt 与编排方法**: 在 `prompt_templates.py`、`model_router.py`、`orchestrator.py`
       中新增 `import_dialogue_parse` 与 `interaction_analysis_rebuild` 任务，让导入流程仍然走 AI
       Harness，而不是绕开到单独的临时脚本。这样导入后的 Interaction Unit 会继续生成与页面1一致的分析层，包括内心独白、情绪归因、策略解释、行为倾向预测和高密度标签。
    4. **新增页面3导入/导出接口**: 在 `backend/app/api/characters.py` 中新增：
        - `POST /characters/imports/preview`：上传文件后生成预览；
        - `POST /characters/imports/commit`：确认映射后批量写入角色、事件、关系、Interaction Unit，并可创建“只读导入会话”；
        - `GET /characters/exports/full`：导出完整 JSON 包（角色 / 事件 / 关系 / 观察 / 导入记录 / 交互单元）。
    5. **新增页面3导入/导出 UI**: 在 `frontend/src/views/AdminDashboard.vue`
       中加入统一智能导入/导出弹层，支持文件上传、角色映射确认、心理语义预览、剧情结构预览、导入选项选择以及全量 JSON 导出。
    6. **建立页面联动**: 导入确认后，系统会把 Interaction Unit 写入追溯表，并可同时创建页面1可读取的会话消息与 AI
       分析消息，实现“导入即对话、导入即分析、导入即建模”的联动链路。
- **为什么要这样修改**:
  之前页面3只能手工维护角色、事件和关系，无法从真实文本或剧本中批量建模，数据也无法回流到页面1形成可对话、可分析的上下文。此次升级后，页面3不再只是管理页，而变成统一的语义理解入口：任何导入内容都会被转成统一协议，再进入角色建模、关系建模和对话系统。
- **利用的技术**: FastAPI 文件上传、Pydantic 导入协议建模、AI Harness Prompt 编排、规则解析与 LLM 混合抽取、前端多步骤预览确认
  UI、全量 JSON 导出。

### 13. 修复前端上传文件预览报错 404 (Not Found)

- **修改说明**:
    1. 检查了后端代码，确认 `backend/app/api/characters.py` 中已经正确添加了 `POST /imports/preview`
       和 `POST /imports/commit` 路由。
    2. 发现 Vite 代理的后端目标端口 8000 上运行的仍然是旧版本的 FastAPI 进程，并未热重载加载最新的路由代码。
    3. 终止了占用 8000 端口的僵尸进程，并使用 `uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload` 重新启动了后端服务。
- **为什么要这样修改**: 前端在调用 `/api/v1/characters/imports/preview` 时返回 404
  错误，通常是因为后端服务中不存在该路由。由于代码中确已编写了该路由，这说明运行中的服务进程与代码状态不同步（可能是之前的代码语法错误导致
  uvicorn 的 watchfiles 挂起或未能正确热重载）。通过彻底杀掉旧进程并重启，确保前端请求能够打到包含最新导入导出路由的后端实例上。
- **利用的技术**: Windows 端口与进程管理 (`netstat`, `taskkill` / `Stop-Process`)，Uvicorn ASGI 服务器重启。

### 14. 修复导入预览“无反应 / 后台不执行 / 数据不出来”的问题

- **修改说明**:
    1. 修改了 `backend/app/harness/import_engine.py`，在不改变原有导入主流程的前提下，为导入预览增加了“规则解析兜底 + AI
       解析超时保护”。现在结构化文件仍走原有结构化解析；对话文本仍先走原有规则拆分，再尝试 AI 增强；叙事文本新增基础规则解析结果，再尝试
       AI 增强。如果 AI 超时、异常或返回格式不稳定，系统会自动保留基础解析结果继续返回，不再让整个预览卡死。
    2. 在同一文件中补充了结果标准化逻辑，统一保证 `characters`、`interaction_units`、`events`、`relationships`
       、`plot_summary` 这些字段始终存在并保持正确类型，避免前端拿到异常结构后无法渲染。
    3. 修改了 `backend/app/api/characters.py` 的 `preview_import` 接口，为文件解析失败、未知异常增加了明确的异常转换，确保前端能收到可展示的错误信息，而不是静默失败。
    4. 修改了 `frontend/src/views/AdminDashboard.vue`，给“上传并预览 / 确认导入 / 导出
       JSON”增加了显式错误处理和状态提示；同时把相关按钮补成 `type=\"button\"` 并加上 `@click.prevent`
       ，避免浏览器默认行为干扰导入交互。现在即使后台异常，页面也会直接显示失败原因；如果 AI 解析被降级，也会显示提示但仍然把基础数据正常渲染出来。
- **为什么要这样修改**: 这次问题的根因不是单一接口不存在，而是导入预览链路对 AI 解析依赖过重，前端又缺少错误兜底。只要 AI
  请求超时、网络抖动、返回格式不稳定，页面就会表现成“点了按钮没反应”或者“一直解析中但没有数据”。这次修改保持了原有 AI
  Harness 主逻辑不变，只是在关键节点增加了稳定性保护和可见反馈，让导入功能在 AI 正常时继续输出增强结果，在 AI
  异常时也至少返回基础解析数据，保证页面3能稳定出数。
- **利用的技术**: Python `asyncio.wait_for` 超时控制、规则解析与 AI 增强混合策略、结果结构标准化、防御式异常处理、Vue 3
  异步请求错误捕获与状态回显。

### 15. 按页面1 / 页面3完整规格审计并修复统一协议与导入联动问题

- **修改说明**:
    1. 修改了 `backend/app/models/sql_models.py`、`backend/app/main.py` 与 `backend/app/schemas/__init__.py`
       ，把页面1与页面3共同使用的“统一消息协议”补齐到消息层：新增 `message_index`、`receiver_id`、`receiver_name`、`intent`
       、`strategy`、`emotion`、`source_type`、`readonly` 等字段，并在启动时自动补齐 SQLite
       缺失列。这样旧库不删表也能升级，页面1与页面3的数据协议开始真正对齐。
    2. 修改了 `backend/app/api/chat.py`
       ，发送消息时会把主接收者、统一协议字段和消息顺序一起写入数据库；补全分析时同步回写意图/策略/情绪；对话列表增加只读导入标记；情绪张力接口增加热力图数据与情绪关键词数据；归档时优先使用消息已记录的接收方，而不是再用模糊猜测。
    3. 修改了 `backend/app/harness/import_engine.py`，在原有三层解析基础上增加：心理标签生成、伪对话上下文重建、关系弱信号推断、人格特征/行为模式抽取。现在导入预览不再只是“角色
       + Interaction Unit”，而是会直接返回 `pseudo_conversation`、`character_modeling`、`psychological_label`
       等桥接数据，让页面3导入结果更接近页面1原生消息流。
    4. 修改了 `backend/app/api/characters.py`，导入确认写入时会把统一协议字段落到 `Message`，把导入会话标记为只读，并把
       Interaction Unit 的心理标签、上下文窗口、导入版本信息一起持久化；导出接口也同步补上会话、消息、版本信息和新增协议字段。
    5. 修改了 `frontend/src/views/ChatUI.vue` 与 `frontend/src/stores/chat.js`，消息发送完成后主动重新加载会话消息，避免前端只停留在
       optimistic 数据而看不到后端真实协议字段；Chat UI 现在可直接展示接收者、意图/策略/情绪、只读对话标记、热力图与情绪词云，并阻止对只读导入会话继续写入。
    6. 修改了 `frontend/src/views/AdminDashboard.vue`，把导入预览增强为可校正的多层面板：支持直接编辑 Interaction Unit
       的意图/策略/情绪，新增关系预览、伪对话上下文预览、人格特征/行为模式预览以及导入版本展示。
    7. 修改了 `frontend/vite.config.js`，把开发代理切换到 `127.0.0.1:8001`，并实际启动了新的后端实例。这样可以避开当前环境中一个无法稳定替换的旧
       8000 端口服务，确保前端页面命中的就是本轮最新代码。
- **为什么要这样修改**: 这次不是单点
  bug，而是“页面1对话协议”和“页面3导入协议”没有完全收敛，导致很多功能虽然表面存在，但链路不闭环：导入后的会话不是严格只读、消息没有统一的接收方/意图/策略/情绪字段、前端
  optimistic 消息不会自动回填真实后端数据、页面3预览也缺少伪对话与人格建模桥接层。此次修改保持 AI Harness
  主架构不变，只把协议、上下文桥接和前端展示补齐，让导入 → 分析 → 写入 → 页面1消费的路径更完整、更稳定。
- **利用的技术**: SQLite 运行时列补齐、统一消息协议建模、伪对话上下文重建、规则解析与 AI 结果桥接、只读会话机制、Vue 3
  数据回填同步、Vite 代理切换与双端联调验证。

### 16. 修复导入弹窗点击空白区域会自动关闭的问题

- **修改说明**:
    1. 修改了 `frontend/src/views/AdminDashboard.vue`
       中统一导入弹窗的遮罩层事件绑定，去掉了 `@click.self="showImportModal = false"`。
- **为什么要这样修改**:
  之前导入弹窗采用“点击遮罩层即关闭”的交互方式，但导入流程本身是一个多步骤操作页面，包含文件选择、预览校正、关系检查、人格建模检查和确认写入。用户在查看或编辑内容时，如果误点空白区域，整个弹窗会被关闭，容易打断当前操作。去掉这个关闭行为后，弹窗只能通过右上角关闭按钮显式关闭，更符合导入流程这种高操作成本场景。
- **利用的技术**: Vue 事件修饰与弹层交互控制。

### 17. 修复导入文件时 AI 解析频繁回退基础规则解析的问题，并统一多 agent 默认走 DeepSeek

- **修改说明**:
    1. 修改了 `backend/app/harness/model_router.py`，把原来只在模块加载时读取一次配置、并且只绑定单一默认 provider
       的路由方式，改成了“按任务实时读取配置并路由”的实现。现在每个 agent / task 都可以通过 `task_providers` 独立指定
       provider，同时仍保留 `default_provider` 作为总兜底。
    2. 在同一文件中补充了 `task_complexities` 与 `task_models` 配置能力。这样多 agent 仍然支持调用不同
       provider、不同模型，但当前默认策略可以统一收敛到 DeepSeek，而不是有的任务走默认 provider、有的任务继续沿用旧的静态模型映射。
    3. 修改了 `backend/app/harness/orchestrator.py`，把原来进程启动时就固定创建单一 OpenAI 客户端的方式，改成按 provider
       动态获取客户端，并按 provider 的 `base_url/api_key` 缓存实例。这样不同 agent 后续仍能切换到
       OpenAI、Ollama、vLLM，但在当前配置下所有默认任务都会稳定调用 DeepSeek。
    4. 修正了 provider 的密钥解析优先级。现在 DeepSeek 会优先读取 `DEEPSEEK_API_KEY` 或配置文件中的 DeepSeek
       key，不再被 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY` 这类无关环境变量错误覆盖。
    5. 修改了 `backend/app/conf/config.yaml`，为主要任务显式指定 `task_providers` 为 `deepseek`
       ，并把导入解析和分析重建任务的默认模型调成更稳定的 `deepseek-chat`，避免导入预览在复杂模型上超时后频繁触发基础规则兜底。
    6. 修改了 `backend/app/harness/import_engine.py`，把导入 AI 解析超时时间从 20 秒提高到 45 秒，减少网络波动或模型响应稍慢时的误判回退。
- **为什么要这样修改**: 这次 bug 的根因不是单纯“DeepSeek 不可用”，而是当前多 agent 架构下的 provider 选择逻辑仍然是旧式的“启动时固定单一
  provider + 单一客户端”。这会导致配置文件里虽然写了 `default_provider: deepseek`，但不同任务未必都真正稳定走
  DeepSeek；同时导入解析任务使用较重模型时也更容易在 20 秒超时后被判定为“AI 解析暂时不可用”。这次修改保持原有 AI Harness
  架构不变，只把 provider 路由、客户端创建和导入任务默认模型策略修正为更稳定的多 provider / 多 agent 方案，并把当前默认统一收敛到
  DeepSeek。
- **利用的技术**: 动态 provider 路由、按 provider 缓存 OpenAI 兼容客户端、任务级模型配置、环境变量优先级隔离、导入链路超时控制优化。

### 18. 继续修复导入预览仍提示“AI 解析暂时不可用”的实际运行时问题

- **修改说明**:
    1. 复查并实际复现了导入预览接口，确认这次不是 provider 没切到 DeepSeek，而是 `import_dialogue_parse` 任务虽然已经走
       DeepSeek，但 `max_tokens` 只有 1024，导致 DeepSeek 返回的 JSON 在 `relationships`
       字段附近被截断，`guardrails.extract_json()` 无法解析，于是系统才继续走了基础规则解析。
    2. 修改了 `backend/app/harness/model_router.py`，新增 `task_max_tokens` 与 `task_temperatures`
       的任务级覆盖能力，不改变原有复杂度路由逻辑，只是在确实需要更大输出的任务上允许单独提高输出上限。
    3. 修改了 `backend/app/conf/config.yaml`，把 `import_dialogue_parse`、`import_narrative_parse` 的 `task_max_tokens`
       提升到 `4096`，把 `interaction_analysis_rebuild` 提升到 `3072`。这样导入解析类任务可以完整返回角色、Interaction
       Unit、事件、关系与剧情摘要，避免 JSON 被截断。
    4. 终止并重启了当前运行中的后端 `8001` 服务和前端 `5173` 开发服务，确保实际运行进程加载的是这次最新代码，而不是旧进程中的旧配置。
- **为什么要这样修改**: 这次的真实根因是“运行中的导入解析任务输出被截断”，不是“DeepSeek 完全不可用”。也就是说，AI
  实际已经返回了正确方向的内容，但因为输出长度不够，最终 JSON 不完整，系统只好误判为 AI 解析失败。修复任务级 `max_tokens`
  后，导入预览就能拿到完整 JSON，不再错误回退。重启当前服务则是为了确保页面实际命中的就是本轮修复后的后端。
- **利用的技术**: 运行时问题复现、DeepSeek 输出截断定位、任务级 token 限额配置、Vite + Uvicorn 服务重启验证。

### 19. 再次加固导入 AI 解析链路，修复大文本与旧服务进程导致的持续回退

- **修改说明**:
    1. 修改了 `backend/app/harness/orchestrator.py`，对 `import_dialogue_parse`、`interaction_analysis_rebuild` 等严格
       JSON 任务增加 `response_format={"type":"json_object"}`，让 DeepSeek 直接按 JSON 模式返回，减少 markdown
       包裹、格式漂移和结构不完整问题。
    2. 修改了 `backend/app/harness/import_engine.py` 的 `safe_ai_import_parse()`
       ，保留原有主流程不变，但新增了多轮降载重试机制：先尝试完整文本解析；如果失败，再自动改用压缩后的关键片段继续调用
       AI；最后再退到更短文本重试。只要任意一轮成功，就不再回退基础规则解析。
    3. 这次实际重新复现后发现，除了之前的 token 截断问题外，运行中的 `8001` 后端进程还因为热重载退出，前端 `5173`
       虽然还在，但代理时会命中异常状态服务，导致前端表现不稳定。为此我重新以稳定方式启动了后端服务，并确认前端代理重新连上最新后端。
- **为什么要这样修改**: 单纯提高 `max_tokens`
  能解决一部分截断问题，但仍然不能完全覆盖“模型偶发格式漂移”“长文本输出不稳定”“热重载进程失效”这几类运行时问题。这次加固的目标是：即使完整文本一次失败，也要自动换成更短、更稳定的
  AI 负载继续重试，尽可能让页面拿到 AI 语义结果，而不是动不动直接显示“AI 解析暂时不可用”。
- **利用的技术**: JSON 模式输出、分级降载重试、长文本压缩重试、前后端服务重启联调验证。

### 20. 强化修复导入阶段 AI 解析不可用，并统一多 agent 默认走 DeepSeek

- **修改说明**:
    1. 修改了 `backend/app/harness/model_router.py`，补上任务级 provider / model / max_tokens / temperature 配置能力。现在每个
       AI 任务都能独立声明走哪个 provider，但在默认策略下全部统一收敛到 `deepseek`，避免因为某些任务仍沿用旧 provider
       导致导入链路不可用。
    2. 修改了 `backend/app/harness/orchestrator.py`，把原来“进程启动时创建单一全局 client”的方式改成按 provider 动态获取并缓存
       client。这样多 agent / 多任务后续仍可切换不同 provider，但当前所有默认任务都会稳定使用 DeepSeek；同时修正了密钥优先级，DeepSeek
       不会再被无关的 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY` 错误覆盖。
    3. 同一文件中对严格 JSON 任务继续保留 JSON 模式输出，并把导入解析任务按文本类型区分为 `import_dialogue_parse`
       与 `import_narrative_parse` 两条链路，避免叙事文本仍被错误套用单一任务名。
    4. 修改了 `backend/app/harness/prompt_templates.py`，补上 `import_narrative_parse` 模板，让叙事导入也能稳定走 AI
       解析，而不是只能退回基础规则解析。
    5. 修改了 `backend/app/harness/import_engine.py`，把导入 AI 解析的超时时间从 20 秒提高到 45
       秒，并增加多轮降载重试：优先尝试完整文本，再尝试压缩行文本和段落压缩文本。只要任意一轮 AI 成功返回有效结构，就不再给前端提示“AI
       解析暂时不可用”。
    6. 修改了 `backend/app/conf/config.yaml`，显式把现有核心任务的 `task_providers` 全部设为 `deepseek`，并把导入解析、分析重建等严格
       JSON 任务默认模型收敛到更稳的 `deepseek-chat`，同时补了这些任务的 token 与温度配置。
    7. 修改了 `backend/requirements.txt`，补充 `openai` 依赖声明，避免后续环境重新安装后因为缺 SDK 导致 AI Harness 直接不可用。
- **为什么要这样修改**: 这次问题的根因不是单一 prompt 或单一接口错误，而是多 agent / 多任务场景下 provider 默认路由、client
  创建方式、密钥优先级和导入任务稳定性共同叠加导致的。虽然配置文件表面写了默认 DeepSeek，但旧代码实际上仍可能被环境变量或旧的静态
  client 机制带偏。此次修改保持原有 AI Harness 主架构不变，只把 provider 路由和导入解析稳定性做实，让系统默认一定走
  DeepSeek，并显著降低导入阶段误退回基础规则解析的概率。
- **利用的技术**: 任务级 provider 路由、按 provider 缓存 OpenAI 兼容 client、JSON 模式输出、长文本分级重试、任务级模型参数配置、依赖补全验证。

### 21. 继续强修导入阶段 TimeoutError，并修正前端实际命中的旧后端问题

- **修改说明**:
    1. 修改了 `backend/app/harness/orchestrator.py` 的导入解析调用，把 `parse_import_content()`
       的内部重试改为 `retries=0`
       。原因是导入链路外层本身已经有多轮降载重试，再叠加内层重试会把总耗时放大成多倍，更容易触发 `TimeoutError`。
    2. 修改了 `backend/app/harness/import_engine.py`，不再优先把原始大文本整段塞给导入解析模型，而是先基于现有规则解析结果构造“紧凑版
       AI 解析输入”：只保留角色摘要、前若干条 Interaction Unit、事件摘要和关键文本片段，再分级降载发送给
       AI。这样在保持原有主逻辑不变的前提下，大幅缩短了导入专用 prompt，避免导入场景独有的超时问题。
    3. 同一文件中重构了 `merge_ai_parse_result()`，把原来“AI 结果一旦返回就整体替换
       interaction_units”的方式，改成按 `source_line_index` / `speaker+content` 对规则解析结果做增量合并。这样 AI
       就不必每次都完整返回整份长文本的所有交互单元，只要返回关键增强字段，也能合并回原始解析结果。
    4. 修改了 `frontend/vite.config.js`，把开发代理重新指回 `127.0.0.1:8001`
       。这次实际复查时发现，当前前端开发服务又在代理到 `8000`，而 `8000` 上跑的并不是这轮最新修复后的后端，所以你在页面里看到的
       TimeoutError 其实是旧服务返回的旧逻辑结果。
    5. 实际终止并重新启动了后端 `8001` 服务与前端 `5173` 服务，确保页面真正命中的是这轮修复后的代码。
- **为什么要这样修改**: 这次之所以让人感觉“明明别的 AI 都正常，偏偏导入老失败”，真实原因有两层：第一层是导入链路本身的
  prompt 比普通聊天长得多，而且外层降载重试和内层 SDK
  重试叠加，确实更容易在导入阶段触发超时；第二层是前端开发代理实际打到了旧的 `8000` 后端，所以你页面上看到的并不是我刚改完的那版代码。这次我没有改动原有
  AI Harness 主体逻辑，只把导入链路的输入压缩、重试层级和代理目标修正到真正稳定可用的状态。
- **利用的技术**: 导入专用 prompt 压缩、增量语义合并、分层重试去重、Vite 代理修正、前后端服务重启联调验证。

### 22. 将导入流程改为后台异步审核入库，并补齐日志、时间线事件与前端完成提示

- **修改说明**:
    1. 修改了 `backend/app/api/characters.py`，把原先同步执行的 `commit_import()`
       改成“提交即排队，后台异步处理”。现在点击“确认导入并写入系统”后，接口会立即返回任务已提交状态，真正的 AI
       审核、分析重建、关系写入和入库动作在后台继续执行，不再阻塞页面。
    2. 同一文件中增加了导入状态查询接口 `/characters/imports/{import_file_id}/status`，并把 `ImportFile.status`
       扩展为 `queued / reviewing / processing / committed / failed` 这套后台任务状态。前端可轮询真实状态，不再依赖单次提交接口卡住等待。
    3. 在同一文件中增加了“导入审核代理”逻辑：正式入库前，会先调用 AI
       审核代理自动检查角色映射、关系、事件与剧情摘要，然后再按审核后的结果自动入库，减少人工干预，但仍保留你原来可手动修改预览内容的能力。
    4. 同一文件中重写了“人物事迹时间线”的导入写法。原来是每条 Interaction Unit 直接生成一条事件，容易变成“某句话 =
       一条事件”；现在会优先把导入里的事件结构或多轮对话聚合成更完整的事件摘要，再写入时间线，记录的是“发生过什么事”，而不是逐句复读。
    5. 修改了 `backend/app/main.py` 与 `backend/app/harness/orchestrator.py`
       ，正式接入后台日志体系：启动时自动创建 `backend/app/logs`
       目录，同时把日志并行输出到控制台和 `backend.log / import.log` 文件中；导入任务的排队、审核、AI
       调用成功/失败、事件生成、任务完成或失败都会落日志，不再出现“只有界面提示，后台没输出”的情况。
    6. 修改了 `backend/app/harness/model_router.py`、`backend/app/harness/prompt_templates.py`
       与 `backend/app/conf/config.yaml`，新增 `import_commit_review` 审核代理任务，并保持与之前一致的模型介入逻辑：仍走 AI
       Harness 路由、仍支持后续切换模型，但当前默认统一使用 DeepSeek。
    7. 修改了 `frontend/src/views/AdminDashboard.vue` 与 `frontend/src/api/index.js`
       。现在“创建只读会话并接入页面1”默认不选；点击确认导入后会自动关闭导入弹窗；前端会后台轮询导入状态；导入完成后会在“关系网络”标题旁边显示“导入完成”提示；同时写入选项区域会明确提示“AI
       审核代理会自动审核并后台入库”。
- **为什么要这样修改**: 这次不是一个单点交互问题，而是导入链路整体还停留在“同步提交 + 人工盯进度 + 逐句生成事件 +
  无后台日志”的模式。这样会带来四个实际问题：一是点击导入时页面容易卡；二是用户看不到后台真实进度；三是时间线事件质量太低；四是出了问题没法从日志里定位。此次修改保持原有
  AI Harness 主架构不变，只把导入流程升级成真正可交付的后台任务模式，并把审核代理、事件聚合、状态查询和日志输出补齐。
- **利用的技术**: FastAPI BackgroundTasks 后台任务、导入任务状态轮询、AI 审核代理、事件聚合摘要、Python logging +
  RotatingFileHandler 双通道日志、前端轮询与完成提示联动。

### 23. 修复“功能已写但界面看起来完全没生效”的遗留数据问题

- **修改说明**:
    1. 复查实际运行结果后确认，这次不是功能代码完全没执行，而是旧导入遗留数据覆盖了新效果：角色已有的旧 `导入事件 #N` 事件仍然保留，人物时间线优先展示这些旧事件；同时旧的 `behavior_pattern` 待审核建议也一直挂在角色档案里，所以界面看起来像“自动审核没有生效”。
    2. 修改了 `backend/app/api/characters.py`，在新的导入任务开始入库前增加了旧导入遗留数据清理逻辑：会清理参与角色历史上由旧逻辑生成的 `导入事件 #N` 事件，并把旧的 `behavior_pattern` 待审核记录自动转为已审核状态。这样新导入生成的聚合事件和自动审核结果才会真正显示出来，不会被旧垃圾数据盖住。
    3. 同一文件中调整了事件列表接口排序，改为优先按 `event_date` 倒序、再按 `created_at` 倒序返回，避免新生成的高质量导入事件被旧空日期事件压到下面。
    4. 修改了 `frontend/src/views/AdminDashboard.vue` 的时间线排序逻辑，当前端拿到事件后也会按 `event_date` 或 `created_at` 做倒序排序，确保最新导入结果优先显示。
    5. 同一文件中在导入完成轮询后补充了 `loadObservations()` 刷新，这样角色档案页不会继续停留在旧的待审核缓存状态。
- **为什么要这样修改**: 这次问题的关键不是“新功能没写进去”，而是“旧数据没清掉，导致新功能效果被旧数据完全遮住”。导入链路虽然已经切成后台异步、自动审核和事件聚合，但如果角色身上还挂着旧版本生成的逐句导入事件和待审核观察，界面看起来就会像根本没改。此次修改保持原有逻辑不变，只补上旧数据清理、排序和界面刷新三个缺口，让新功能的结果真正能被看到。
- **利用的技术**: 导入遗留数据清理、SQLAlchemy 条件更新、时间线排序修正、前端状态刷新同步。

### 24. 对旧数据库中的历史导入遗留垃圾数据执行一次全量清洗

- **修改说明**:
    1. 直接对当前数据库执行了一次全量清洗操作，没有改动业务逻辑文件，只清理历史导入过程中留下的明确垃圾数据。
    2. 删除了所有 `ImportFile.status` 为 `previewed / failed / queued / reviewing / processing` 的旧导入记录，共清理 `38` 条。这些记录都属于未完成、预览残留或中断任务，不是最终导入结果。
    3. 删除了所有由旧导入逻辑自动生成的 `behavior_pattern` 观察记录，共清理 `6` 条。这些记录的特征是 `reason` 以“导入文本识别到重复行为模式：”开头，属于历史导入产生的冗余建议数据。
    4. 同时复查了旧格式的 `导入事件 #N` 事件，本次数据库中已为 `0` 条，因此无需额外删除。
    5. 清洗后再次验证：未完成导入记录为 `0`、旧导入事件为 `0`、旧导入行为模式观察为 `0`，保留的正式 `committed` 导入记录还有 `9` 条，说明清洗只删除垃圾数据，没有误删正式导入结果。
- **为什么要这样修改**: 你这次要求的是“对旧数据库里所有历史导入遗留垃圾数据做一次全量清洗”，所以这一步应该直接落到数据库本身，而不是继续停留在代码层。此次处理的原则是：只清理明确属于导入过程残留、又不会构成正式业务资产的数据，避免误删真正已经提交成功的导入结果。
- **利用的技术**: SQLAlchemy 数据清洗脚本、按状态筛选导入记录、按 reason 模式筛选历史导入观察记录、清洗前后计数校验。

### 25. 从根本上修复导入预览再次出现 TimeoutError 的问题

- **修改说明**:
    1. 复查当前实现后确认，问题根因不在 DeepSeek 本身，而在于 `/imports/preview` 仍然是同步长耗时请求：前端点击预览后，会一直等待后端完成 AI 解析；而后端内部还可能串行尝试多轮 AI 解析 payload，最终把整个 HTTP 请求拖到超时。为此，我修改了 `backend/app/api/characters.py`、`backend/app/harness/import_engine.py` 和 `frontend/src/views/AdminDashboard.vue`，把“导入预览”从同步等待改为“基础预览立即返回 + AI 增强后台异步完成 + 前端轮询刷新”。
    2. 修改了 `backend/app/harness/import_engine.py`，给 `build_import_preview()` 增加 `enable_ai` 开关。现在预览接口第一次只生成基础规则预览，不在单个 HTTP 请求里阻塞等待 AI；后台任务再调用同一个函数的 AI 增强模式，完成后把增强结果写回数据库。
    3. 修改了 `backend/app/api/characters.py`，新增后台函数 `_enhance_import_preview()`。预览接口返回后，它会在后台继续跑 AI 增强，并把最新 `preview_payload`、`preview_warning` 和状态 `preview_ready / preview_processing` 写入 `ImportFile.metadata_json`。这样即使 DeepSeek 比较慢，前端也不会再因为等一个长请求而报 `TimeoutError`。
    4. 同一文件中扩展了 `/characters/imports/{import_file_id}/status` 返回结构，增加 `preview_payload` 与 `preview_warning`，让前端可以轮询预览增强状态，而不是只能等待一个同步请求直接返回最终结果。
    5. 修改了 `frontend/src/views/AdminDashboard.vue`，新增 `importPreviewPending` 与 `pollPreviewStatus()`。现在点“上传并预览”后，页面会先拿到基础预览并立刻展示；如果 AI 还在增强，就自动轮询状态，增强完成后自动刷新预览数据。同时在 AI 预览增强未完成前，确认导入按钮会被禁用，避免用户拿着半成品预览直接入库。
    6. 修改了 `e:\python\conda\B2B\start.bat`，启动前会自动清理占用 `5173 / 8001` 的旧开发进程，避免你反复启动时前端或后端命中旧实例，导致“明明已经修改但页面还是旧逻辑”的情况。
- **为什么要这样修改**: 这次必须从根上处理，而不是继续调大 timeout。只要“预览接口 = 单个同步长请求”这个结构不变，就算从 60 秒调到 120 秒、180 秒，也只是把问题往后拖，迟早还会超时。真正的根治方案，是把导入预览拆成“快速基础预览”和“后台 AI 增强”两段，让 AI 的慢响应不再卡死浏览器请求链路。这样前端不会再因为等待 DeepSeek 响应而报 `TimeoutError`，同时仍然保留 AI 增强效果。
- **利用的技术**: 后台异步预览增强、预览状态轮询、FastAPI BackgroundTasks、ImportFile 元数据回写、前端状态驱动 UI、启动脚本端口清理。

### 26. 按最终统一方案补齐导入质量、AI自动更新展示与角色档案/编辑页统一

- **修改说明**:
    1. 修改了 `backend/app/harness/import_engine.py`，把长文本基础解析改成“分块读取 + 顺序重组”模式：新增 `chunk_text()`，并在对话文本与叙事文本场景下分别引入 `parse_dialogue_chunks()` 和 `parse_narrative_chunks()`。这样长文本不会再只靠单次整段解析，能更稳定保留上下文和顺序。
    2. 同一文件中重做了标签生成逻辑：`build_psychological_label()` 不再直接随便取一个字段，而是基于 `intent + strategy + emotion` 组合生成，并对低置信度结果进行过滤。平均置信度低于 `0.6` 的标签直接不展示，避免出现杂乱或误导标签。
    3. 同一文件中增强了导入侧人格建模：`extract_character_modeling()` 现在不只返回行为模式名字，还会带上置信度、触发条件、示例和重复次数，便于后续写入角色档案和页面展示。
    4. 修改了 `backend/app/api/characters.py` 与 `backend/app/schemas/__init__.py`，新增行为模式的创建、更新、删除接口与结构体，让“编辑页 = 展示页的可编辑版本”真正成立。现在行为模式不再只是展示卡片，而是可由用户直接新增、编辑和删除。
    5. 同一文件中把导入生成的行为模式观察记录从原来的简单 reason 文本，改成结构化可读说明：包含来源、置信度、触发、示例。这样 UI 能准确展示“来源：导入分析 / AI自动识别”“置信度”“触发条件”“示例”等信息。
    6. 修改了 `frontend/src/views/AdminDashboard.vue`，把角色档案页重构为更清晰的 5 大块：基础信息、人格模型、行为模式、AI 自动更新、已有的关系与时间线；同时把“AI建议更新”文案调整为“AI自动更新”，展示“自动应用（N）/ 待确认（N）”，不再给人“明明已应用却还像待审核”的错觉。
    7. 同一文件中重构了编辑弹窗：新增“人格特征（逗号分隔）”与“行为模式（可编辑）”区域，支持用户直接编辑核心动机、心理弱点、人格标签、行为模式来源/置信度/触发/示例，并在保存时同步到后端。
    8. 修改了 `frontend/src/api/index.js`，补齐前端对行为模式 CRUD 的调用封装，使展示页和编辑页共用同一套角色档案数据源，不再是“展示一套、编辑另一套”的分裂状态。
- **为什么要这样修改**: 你这次给出的最终统一方案，本质上要求把“导入质量”“AI自动更新”“角色档案展示”“编辑控制”统一成一套闭环。之前的问题不是单一字段缺失，而是导入系统、AI输出、展示页和编辑页各用各的逻辑，导致 AI 有结果但 UI 看不到，或能看到却不能编辑。此次修改保持原有页面结构与后端主接口不变，只补齐长文本分块解析、标签生成约束、行为模式结构化存储和前后端统一编辑能力，让 AI 结果真正做到“可生成、可展示、可编辑、可修正”。
- **利用的技术**: 长文本 chunk 解析、伪对话顺序重组、标签置信度过滤、结构化行为模式元数据、前后端统一 CRUD、角色档案/编辑页同源化设计。


### 27. 停止导入预览实时监测并修复 AI 增强超时降级

- **修改说明**:
    1. 修改了 `frontend/src/views/AdminDashboard.vue`，停止导入预览阶段对 `/api/v1/characters/imports/{id}/status` 的自动实时轮询。现在 AI 增强进入后台后，不再每隔 1.5 秒自动请求状态，而是改为提示“已停止实时监测”，由用户手动点击“手动刷新 AI 结果”查看增强结果。
    2. 同一文件中把导入弹窗右上角关闭按钮改为统一走 `closeImportModal()`，关闭弹窗时会主动清理已有的轮询定时器，避免弹窗关闭后仍残留状态请求。
    3. 修改了 `backend/app/harness/import_engine.py` 的 `safe_ai_import_parse()`，把 AI 解析超时从原来继续串行尝试后续 payload，改为一旦命中 `TimeoutError` 就立即停止本次 AI 增强并直接降级到基础规则解析。
    4. 同一文件中将单次 AI 解析等待上限从 `60s` 下调到 `35s`。这样可以更快识别出本轮请求已不适合继续等待，避免后台长时间卡在一次无效的 AI 增强上。
- **为什么要这样修改**: 这次报错里最明显的问题有两个：一是前端会持续请求 `/imports/{id}/status` 做实时监测；二是后端 AI 解析一旦超时，还会继续尝试后续 payload，导致无意义等待更久。你这次明确要求“停止实时监测”，所以前端改成手动刷新模式；同时后端在 AI 超时时立即降级，不再把一次已经超时的增强任务继续拖长。这样既减少控制台刷屏，也减少无效等待。
- **利用的技术**: 前端手动刷新替代轮询、定时器清理、异步任务状态一次性查询、`asyncio.TimeoutError` 快速降级、超时阈值收紧。

### 28. 修复 AI 增强被过早判定超时导致“无法连接”

- **修改说明**:
    1. 复查 `frontend/src/views/AdminDashboard.vue` 后确认，当前 `/api/v1/characters/imports/{id}/status` 在“导入预览”阶段已经不再自动实时轮询，只剩“手动刷新 AI 结果”会调用一次；正式导入提交后的后台状态轮询仍然保留。也就是说，这次“无法连接”的直接原因不在前端状态请求本身。
    2. 问题根因在于我上一次把 `backend/app/harness/import_engine.py` 中 `safe_ai_import_parse()` 的 AI 等待上限从 `60s` 收紧到了 `35s`。这个改动过于激进，导致一些正常但稍慢的 AI 增强请求被提前打成 `TimeoutError`，看起来像“连不上”。
    3. 现已把该逻辑改为按导入文本长度动态设置等待上限：默认 `70s`，中等文本 `90s`，超长文本 `110s`。同时仍保留“命中超时后立即停止本次 AI 增强并降级”的逻辑，避免无限拖长。
    4. 超时提示也同步改成携带本次等待上限，便于后续从日志里直接看出是哪个阈值触发了降级。
- **为什么要这样修改**: 你这次说“修改了什么导致无法连接”，我排查后确认就是上次为了压缩等待时间，把 AI 超时阈值收得太紧了。停止无效重试这个方向没问题，但把等待上限直接砍到 `35s` 会误伤正常慢请求。所以这次保留“超时即停止”的原则，同时把阈值恢复成更合理的动态区间，既不让请求无限卡住，也不再过早误判失败。
- **利用的技术**: 动态超时阈值、按文本长度估算等待时间、快速失败保留、日志可观测性增强。

### 29. 仅补充文档：导入步骤详细流程、5536 字文本为什么仍然慢、后续修改建议

- **导入步骤详细流程**:
    1. **前端上传文件**：页面点击“上传并预览”后，前端把原始文件提交到 `/api/v1/characters/imports/preview`。这一阶段只负责“生成预览”，还没有真正写入角色、关系、事件和分析层。
    2. **后端读取文件二进制**：`preview_import()` 先通过 `await file.read()` 读出完整文件内容，然后调用 `extract_text_from_file()` 按扩展名解析文本。TXT / MD / JSON / CSV 直接解码，DOCX 用 `python-docx`，PDF 用 `pypdf`。也就是说，真正参与后续分析的是“提取后的纯文本”，不是原始文件大小本身。
    3. **识别导入类型**：`detect_import_type()` 会根据文件扩展名和内容是否存在“角色名：内容”这类对话标记，把文件分成 `structured / dialogue / narrative` 三类。JSON、CSV 直接走结构化解析；TXT、MD、PDF、DOCX 会进一步判断更像对话还是叙事。
    4. **先做基础规则解析**：`build_import_preview(..., enable_ai=False)` 不会直接先等 AI，而是先做一轮本地规则解析。对话文本会走 `parse_dialogue_chunks()`，叙事文本会走 `parse_narrative_chunks()`，这里已经会做分块读取、角色提取、交互单元提取、事件草稿提取。
    5. **生成基础预览结构**：规则解析完成后，`finalize_import_preview()` 会继续补齐预览结果，包括：心理标签 `psychological_label`、推断关系 `relationships`、伪对话 `pseudo_conversation`、人格建模 `character_modeling`、8 大模块档案 `character_profiles`、角色映射 `role_mappings`、低置信度单元 `low_confidence_units`。也就是说，即使 AI 没跑完，页面也不是空的，而是已经有一轮基础预览。
    6. **保存 ImportFile 记录**：`preview_import()` 会把这次预览写入 `ImportFile` 表，并把 `preview_payload`、计数信息、版本信息写进 `metadata_json`。如果文件不是结构化数据，则状态会先记成 `preview_processing`，表示“基础预览已就绪，但 AI 增强还在后台跑”。
    7. **后台触发 AI 增强**：`background_tasks.add_task(_enhance_import_preview, ...)` 会在返回基础预览后继续跑 AI。这里真正消耗时间的是 `build_import_preview(..., enable_ai=True)` 内部的 `safe_ai_import_parse()`。
    8. **AI 增强前先构造候选输入**：`safe_ai_import_parse()` 不会简单把全文直接扔给模型，而是先做一轮候选载荷构造。`build_import_ai_candidate_payloads()` 会生成最多 3 组输入：  
       - 一组是“规则解析结果 + 头中尾文本窗口”的紧凑 JSON  
       - 一组是“规则解析角色/交互/事件摘要 + 文本摘要”的纯文本说明  
       - 一组是纯文本窗口拼接摘要  
       也就是说，AI 并不是只看 5536 个字符，而是要看“文本 + 规则提取摘要 + 强约束任务说明”这一整套上下文。
    9. **AI Harness 发起强约束解析**：`orchestrator.parse_import_content()` 会调用 `import_dialogue_parse` 或 `import_narrative_parse` 提示词。这里的提示词里已经包含非常长的 8 大模块 JSON 模板、必填字段、频次限制、证据要求、事件条数要求、关系强度要求。模型不只是“读文本”，而是在做“强约束结构化人格建模”。
    10. **AI 返回后经过护栏校验**：`model_router.validate()` 会检查是否缺模块、行为模式频次是否不足、动机/弱点证据是否不足、时间线是否少于 5 条。只要不满足这些约束，这一轮 AI 返回就会被判定为无效。即使原文不长，只要输出不符合约束，也会被视为这轮增强失败。
    11. **AI 增强结果回写预览状态**：如果增强成功，`_enhance_import_preview()` 会把增强后的 `preview_payload`、计数和 warning 写回 `ImportFile.metadata_json`；如果超时或异常，则会把 warning 写回，并保留基础预览。
    12. **真正导入提交时还会做更多事**：点击“确认导入并写入系统”后，才会进入 `_process_import_commit()`。这一步并不是简单存一份 JSON，而是继续执行：  
       - AI 审核代理 `_auto_review_import()`  
       - 角色匹配 / 新建角色 `_ensure_character_from_mapping()`  
       - 清理旧导入垃圾数据 `_cleanup_legacy_import_artifacts()`  
       - 把 8 大模块档案写入角色主档 `_apply_character_profiles()`  
       - 生成角色事件 `_create_import_events()`  
       - 对每条 interaction_unit 再调用一次 `orchestrator.rebuild_import_analysis()` 重建分析层  
       - 写入 InteractionUnit / Message / Relationship / CharacterEvent / Observation  
       所以“预览慢”和“正式导入更慢”是两套不同层级的耗时。

- **为什么 5536 字、90 行看起来不大，却还是慢**:
    1. **当前慢点不在文件行数，而在 AI 任务复杂度**。5536 字对本地字符串处理并不大，规则解析、分块、关系推断这些 Python 本地逻辑都不算重。真正慢的是 AI 解析步骤，因为模型收到的不是“5536 字原文”这么简单，而是“原文摘要 + 规则摘要 + 超长结构模板 + 强校验要求”。
    2. **5536 字不会被当前代码判定为“大文件”**。按当前 `safe_ai_import_parse()` 的逻辑，`content_length = 5536` 时不会进入 `>6000` 和 `>12000` 分支，而会走默认等待上限。也就是说，从代码判断标准看，这个长度本身不算大文件。
    3. **真正变大的是“提示词总上下文”**。虽然文本本身 5536 字，但提示词里还带了完整的 CharacterProfile 8 大模块 JSON 模板、强制规则、关系/事件/行为模式约束；再叠加规则解析出来的角色、交互、事件摘要后，模型实际要处理的 token 量远高于 5536 字文本本身。
    4. **当前任务属于高约束生成，不是普通摘要**。模型不是只总结，而是要同时输出：角色、交互、事件、关系、plot_summary、character_profiles；其中 `character_profiles` 里每个角色还要补 8 大模块。这个任务比单纯“总结 90 行文本”要重很多。
    5. **AI 还要满足护栏，不是返回点东西就算成功**。如果模型输出里缺字段、frequency 不够、evidence 不够、event_timeline 不够，结果就会被视为不合格。即使模型已经花时间返回了，也可能因为结构不达标而“这轮无效”。
    6. **导入不是单一模型调用**。预览阶段至少包含：文件读取 → 规则解析 → 结构补齐 → 后台 AI 增强。正式提交阶段还会继续做审核代理、逐条 interaction 的分析重建、关系与事件入库。所以你看到的“慢”，经常不是单个函数慢，而是整条链路累积。
    7. **对于 5536 字文本，当前 270 秒默认超时偏宽，但也说明 AI 任务本身确实重**。也就是说，它不是“因为文件太大所以慢”，而是“因为当前要求模型做的事情太多，所以即使文本不大也可能慢”。

- **当前真正的问题点**:
    1. **文件大小判断指标过粗**：当前只按字符数判断等待上限，没有区分“文本短但角色多、关系多、约束重”的情况。
    2. **预览阶段承担了过多目标**：预览不仅要给角色、关系、事件，还要尝试生成完整 8 大模块档案，导致预览本身就很重。
    3. **提示词太长、要求太满**：强约束模板解决了字段缺失和浅理解，但也显著增加了输出成本。
    4. **候选载荷有多份，但每份都不轻**：虽然是为了提高成功率，但实际上每次候选输入都已经足够复杂。
    5. **正式导入阶段逐条 interaction 重建分析非常慢**：`_process_import_commit()` 里对每一条 `interaction_unit` 都会再调一次 `rebuild_import_analysis()`，文本不长时你可能感觉不到，但稍微一多就会变成线性放大。
    6. **数据库写入频繁**：当前导入提交里有大量 `db.add() -> db.commit() -> db.refresh()` 的小步提交，虽然安全，但速度不友好。

- **详细修改建议（仅建议，不改代码）**:
    1. **把“预览目标”与“完整建模目标”拆开**：  
       建议把“导入预览”只保留角色、关系、事件、基础交互单元，让页面先可用；完整 8 大模块 CharacterProfile 放到“增强档案”按钮或后台二阶段生成。  
       **原因**：预览的核心是给用户看“导不导、映射对不对”，不是第一时间把所有人格建模做完。
    2. **把 8 大模块分两轮 AI 生成**：  
       第一轮只产出 `characters / interaction_units / events / relationships / plot_summary`；第二轮再基于第一轮结果生成 `character_profiles`。  
       **原因**：把“结构识别”和“人格建模”拆开后，模型每轮任务更聚焦，失败点更少。
    3. **缩短预览阶段提示词**：  
       建议预览阶段不要把完整的强约束模板全部塞给模型，而是改成“轻量版预览模板”；只有正式建模阶段再用完整版 8 大模块模板。  
       **原因**：现在慢的主要不是 5536 字文本，而是超长系统提示词 + 超长输出模板。
    4. **把“字符数阈值”改成“估算 token / 角色数 / 交互数联合阈值”**：  
       建议不要再只用 `content_length` 判断是否慢，而是结合：角色数、interaction_units 数、events 数、候选载荷字数。  
       **原因**：5536 字文本如果角色很多、交互密度高，实际任务复杂度依然高。
    5. **减少候选载荷数量**：  
       如果第一候选载荷已经包含“头中尾文本 + 规则摘要”，建议默认只跑这一组；只有明确结构缺失时再尝试第二组。  
       **原因**：现在候选载荷存在一定重复信息，增加了请求准备和失败兜底复杂度。
    6. **预览阶段限制输出规模**：  
       建议给 `character_profiles` 增加上限，例如：最多输出前 6 个重点角色、每个角色最多 3 条行为模式、最多 5 条事件。  
       **原因**：模型生成越多、越细，速度越慢，且更容易触发结构不稳定。
    7. **正式导入阶段改为批量写库**：  
       建议把 `_create_import_events()`、`_process_import_commit()` 中的多次 `commit/refresh` 改成阶段性批量提交。  
       **原因**：当前大量小事务提交会显著拖慢整体导入。
    8. **逐条 interaction 的 AI 重建改为分批或抽样**：  
       建议不要对每条 interaction 都单独调用 `rebuild_import_analysis()`；可以改为批量分析，或者只对高价值单元重建。  
       **原因**：这一步是正式入库阶段最大的线性耗时点之一。
    9. **增加“性能日志拆分”**：  
       建议日志里单独记录每个阶段耗时：  
       - 文件文本提取耗时  
       - 规则解析耗时  
       - AI 候选载荷构造耗时  
       - AI 请求耗时  
       - 护栏校验耗时  
       - 档案生成耗时  
       - 入库耗时  
       **原因**：现在只看到“总慢”，还不够精确，不利于判断到底是模型慢、护栏慢，还是 DB 慢。
    10. **增加“预览模式选择”**：  
        建议在导入页面给用户两个按钮：  
        - 快速预览：只做规则预览  
        - 深度预览：再跑 AI 强约束档案增强  
        **原因**：不是所有导入都需要一上来就跑完整人格建模。

- **结论**:
    1. `5536` 字、`90` 行，从文本体量上看并不算大文件。
    2. 当前慢，不是因为“文件太大”，而是因为“导入预览这一步让 AI 做的事情太多”：要看摘要、读规则结果、满足 8 大模块强约束、输出完整结构。
    3. 如果后续要真正把速度降下来，最有效的方向不是继续纠结字符数阈值，而是**把预览链路拆轻、把强约束人格建模延后、把逐条 AI 重建和频繁 commit 优化掉**。

### 30. 按导入优化文档落地：压缩预览阶段 AI 负载，修复 JSON 半截输出

- **修改说明**:
    1. 修改了 `backend/app/harness/prompt_templates.py`，把 `import_dialogue_parse` 和 `import_narrative_parse` 的系统提示词从超长示例模板，改成更紧凑的字段约束说明。保留了 8 大模块和必填字段要求，但去掉了大段重复 JSON 示例，减少模型在输出前消耗的上下文预算。
    2. 同一文件中新增了预览阶段的输出规模限制：角色最多 `6` 个、交互最多 `18` 条、事件最多 `8` 条、关系最多 `10` 条、角色档案最多 `6` 个；每个角色的人格特征、情绪模式、行为模式、动机、弱点、关系、事件时间线也都增加了上限。这样模型在预览阶段不需要一次性输出过长 JSON，能显著降低被截断成半截的概率。
    3. 修改了 `backend/app/harness/import_engine.py` 的 `build_import_ai_candidate_payloads()`，把预览 AI 输入默认收敛为“一个主候选载荷优先”。只有当规则解析结果明显过 sparse（如角色太少、交互太少、叙事无事件）时，才补充次级候选载荷和纯文本摘要。这样减少了重复上下文与重复尝试，符合前面文档里“减少候选载荷数量”的建议。
    4. 修改了 `backend/app/harness/orchestrator.py`，导入解析任务的第一次请求会自动把 `max_tokens` 提升到至少 `8192`，重试时提升到至少 `12000`；同时如果检测到 `finish_reason == "length"`，会直接判定为“输出被长度截断”并走重试，而不是把半截文本当成普通 JSON 失败。
    5. 同一文件中对导入解析重试增加了“压缩结果规模、优先保留核心角色与关键事件、确保 JSON 完整闭合”的二次约束，避免第二次还按第一次的输出规模继续写过长 JSON。
    6. 修改了 `backend/app/harness/model_router.py` 的 `extract_json()`，当检测到花括号或中括号无法闭合时，会明确标记为“模型 JSON 输出疑似被截断”，便于日志快速定位，不再只是泛化成普通 JSON 解析失败。
- **为什么要这样修改**: 这次日志里的核心问题，不是 AI “完全不能用”，而是输出被截断成了半截 JSON：从 `personality_tags` 数组开始就没闭合。根因正好对应前面文档里写到的几个问题：预览阶段让 AI 同时做太多事、提示词太长、输出结构太大、候选载荷偏多。此次修改保持原有导入主流程不变，只收敛“预览阶段 AI 增强”的输入和输出规模，同时提升导入任务的输出 token 预算，让模型更容易一次性返回完整 JSON。
- **利用的技术**: 紧凑提示词、预览结果限流、候选载荷收敛、动态提升 `max_tokens`、基于 `finish_reason` 的截断检测、JSON 闭合性检测、失败后二次压缩重试。

### 31. 按方案全面落地：预览链路拆轻、强约束人格建模延后、逐条 AI 重建与频繁 commit 优化

- **修改说明**:
    1. 修改了 `backend/app/harness/prompt_templates.py` 和 `backend/app/harness/model_router.py`，将 `import_dialogue_parse` / `import_narrative_parse` 的预览阶段任务从“直接要求返回完整 8 大模块角色档案”改为“只返回轻量预览核心结构”：`characters`、`interaction_units`、`events`、`relationships`、`plot_summary`。同时新增 `import_profile_synthesis` 任务，专门负责在后续阶段生成完整 8 大模块角色档案。
    2. 修改了 `backend/app/harness/orchestrator.py`，新增 `synthesize_import_profiles()`。现在导入预览阶段不再让 AI 一次完成“结构识别 + 完整人格建模”，而是先做轻量识别；正式导入提交时，再基于预览摘要单独调用强约束档案建模任务，落实“强约束人格建模延后”。
    3. 修改了 `backend/app/harness/import_engine.py` 的 `build_import_preview()` / `finalize_import_preview()`，增加 `include_character_profiles` 开关。当前前台预览和后台预览增强都走轻量模式，不再在预览阶段强行生成完整 `character_profiles`，只保留轻量基础预览结果。
    4. 修改了 `backend/app/api/characters.py` 的 `_process_import_commit()`，在真正写库前先调用 `_ensure_strong_character_profiles()`。也就是说，用户点击“确认导入并写入系统”后，系统才会基于预览结果补做完整 8 大模块档案，并继续调用 `_apply_character_profiles()` 写入角色主档。这样把最重的人格建模从“预览阶段”延后到了“提交阶段”。
    5. 同一文件中新增了 `_build_fast_import_analysis()` 和 `_should_run_deep_import_analysis()`。正式导入阶段不再对每一条 `interaction_unit` 都调用 `orchestrator.rebuild_import_analysis()`；现在只对前几条、低置信度条目、缺少心理标签条目、冲突性条目做深度 AI 重建，其余条目走本地快速分析，落实“逐条 AI 重建优化掉”。
    6. 同一文件中优化了 `_create_import_events()`、会话消息写入、关系写入、`InteractionUnit` 写入逻辑，把多处 `db.add() -> db.commit() -> db.refresh()` 改成更少的 `flush()` + 阶段性 `commit()`。这样减少了小事务频繁提交，落实“频繁 commit 优化掉”。
- **为什么要这样修改**: 你选中的方案核心不是“再调几个超时参数”，而是重构导入的职责边界。之前最重的问题在于：预览阶段同时承担了轻量预览和完整人格建模两种目标，导致 AI 负载重、输出长、容易截断；正式提交阶段又对每条互动逐条重建 AI 分析并多次 commit，导致导入越长越慢。这次的改法保持原有导入主逻辑不变，但把最重的人格建模延后到提交阶段，把逐条深度分析收敛到关键条目，把数据库提交改成更少批次，从而同时解决预览阶段过重和提交阶段线性放大的问题。
- **利用的技术**: 任务拆分、两阶段建模、轻量预览模式、延迟强建模、按需深度分析、快速本地分析、`flush()` 替代频繁 `commit()`、阶段性批量提交。
